import { Typography } from '@mui/material';
import { FC } from 'react';

const Dashboard: FC = () => {
  return (
    <Typography
      variant="headline2"
      textAlign={'center'}
      flexGrow={1}
      display="flex"
      justifyContent={'center'}
    >
      DASHBOARD HOME PAGEE
    </Typography>
  );
};

export default Dashboard;
